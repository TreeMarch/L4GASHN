<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chi tiết cây xăng</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      main {
        margin-top: 20px;
        padding: 20px;
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
    </style>
  </head>
  <body class="bg-gray-100">
    <%- include('../partials/header') %>
    <main class="p-6 max-w-4xl mx-auto bg-white rounded shadow-md">
      <h1 class="text-2xl font-bold mb-4">Chi tiết cây xăng</h1>
      <div class="mb-4">
        <p><strong>Tên cây xăng:</strong> <%= station.station_name %></p>
        <p><strong>Địa chỉ:</strong> <%= station.station_address %></p>
        <p><strong>Vĩ độ:</strong> <%= station.station_latitude %></p>
        <p><strong>Kinh độ:</strong> <%= station.station_longitude %></p>
        <p><strong>Thương hiệu:</strong> <%= station.brand %></p>
        <p><strong>Thời gian:</strong> <%= station.open_hours %></p>
        <p><strong>Dịch vụ:</strong> <%= station.services %></p>
        <p><strong>Liên hệ:</strong> <%= station.contact_number %></p>
        <p><strong>Trạng thái:</strong> <%= station.status %></p>
      </div>
      <a href="/" class="text-blue-500 hover:underline">Quay lại danh sách</a>
      <a
        href="/edit/<%= station.station_id %>?_method=PUT"
        class="text-green-500 hover:underline"
        >Chỉnh sửa</a
      >
    </main>
  </body>
</html>
